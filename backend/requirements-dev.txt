# Geliştirme ve CI ortamı için araçlar — production imajına dahil edilmez
# Kurulum: pip install -r requirements-dev.txt

# Lint / format
ruff==0.11.13

# Test
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.27.0
